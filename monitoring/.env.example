# Phiên bản ELK stack
ELK_VERSION=9.1.2

# PostgreSQL local (docker compose up -d postgres ở thư mục jari/)
# Logstash chạy trong Docker → dùng host.docker.internal để tới Postgres trên host
JARI_JDBC_URL=jdbc:postgresql://host.docker.internal:5432/jari
JARI_DB_USER=jari
JARI_DB_PASS=jari
