# Backend URL (backend chạy trên host)
BASE_URL=http://localhost:8080

# Tuỳ chọn — nếu app.security.bypass=false thì bắt buộc
# AUTH_EMAIL=user@example.com
# AUTH_PASSWORD=Password123@

# Tuỳ chọn — bỏ qua auto-discover nếu đã biết ID
# WORKSPACE_ID=
# PROJECT_ID=
# ISSUE_ID=

# smoke | read_dashboard | mixed_workload | all (mặc định: all)
K6_SCENARIO=all
